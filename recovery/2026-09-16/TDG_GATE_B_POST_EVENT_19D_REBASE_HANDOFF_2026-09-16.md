# Complete TDG — Gate B post-event 19D rebase handoff

**Date:** 2026-09-16 (America/Toronto)  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.  
**Restart authority:** `CURRENT_FRONT_LATEST.md` plus `checkpoints/SENTRY/2026-09-15/TDG_GATE_B_POST_EVENT_FRONTIER_19D_REBASE_AND_QB1852_EXECUTABILITY_GAP_2026-09-15.md`.

## Binding latest-wins state

- Root1 frozen; `A_path` retired; global Q uncollapsed; factual ledger separate/append-only.
- Born NOT DERIVED; metric duration NOT DERIVED; Lambda OPEN.
- ELGC remains author-approved working postulate, not old-Root theorem.
- O(3) FAIL/NOT CLOSED; HDA/spin-2/full nonlinear GR NOT ESTABLISHED.
- No preferred z, fitted transport/coefficient, microscopic J, metric projector, tetrad or ADM/EH target.

## Gate B already closed upstream

1. `boundary(Kstar)={T0} disjoint_union others`, `|others|=16`.
2. The 16 first events are exactly the complete first-extension subensemble preserving `T0`.
3. Q-B1819/FP gives the exact conditioned physical lens measure
   `mu_T0(C|rho)=Tr(rho P_C)/Tr(rho B_T0)`.
4. `A+B->C` predictive descent fixes the post-event predictive class `Phi_(e_C)(A)` for each fully typed legal event.
5. Base `A21=P_l1 C_Q P_l2` survives the retained b4/b24, Cons/path, spectator and leading quotient/projective repairs strongly; it may not be hand-deleted.
6. No proper nonzero partial quotient of the irreducible `l=1/l=2` sectors can preserve the full Q-derived SO(3) action.

## New exact correction — 16 first lenses, 19D conditional successor shells

The retained carrier boundary definition is the set of B4 faces with maximal-cell multiplicity one.

A fresh-vertex extension along one frontier B4:

- removes that old attachment face from the frontier (`-1`);
- creates the four other B4 faces of the new 5-cell (`+4`);
- therefore changes frontier size by exactly `+3`.

Since `Kstar` has 17 frontier contexts and every Gate-B first move preserves `T0`:

- each post-event `K_C=Kstar+[moves[C]]` has exactly 20 frontier B4 contexts;
- each post-event common-`T0` successor shell has exactly 19 elementary directions.

Thus:

- number of initial first-event lenses: **16**;
- dimension of each conditional post-event successor shell: **19**.

Do not reuse the old base `F 16x16`, `V 68x16` or old sixteen-context `Apoly` after the event.

## Existing source support

Persisted `qb1860bg_formal_prolongation.py` defines carrier-generic `getGseries(K)`, recomputing boundary, Cons and Gram response data for an arbitrary carrier.

Historical GR21 source explicitly prepares:

- base cut;
- cuts after each first move;
- one-step probes from each post-first-move cut.

Therefore post-event Q/Cons/Gram reevaluation is already typed and does not require a new physical law.

## Exact executable source gap

The exact Q-B1852 source is provenance-pinned as

`source/repro_qb1852/qb1852_full_refined.py`

SHA-256

`ebbe56f7d9c0b19b5c7e688e2b69c7acd2f9a271d161cf558e50e52cad86b5c7`.

The D2 full handoff archive that verified it had archive SHA-256

`5cfa4c1631dbe9204ddcb6e51f2eac9879f377a0c0f6262aa0204f9c28c794f5`.

Current Library/repo audit recovered multiple independent provenance references and later verifier statements that the parent chain contains the source, but not a directly materializable copy of the raw source bytes or parent tar.

The large grandmother recovery references the exact source/hash but does not embed `class RefinedQ` verbatim.

Classification:

**QB1852 RAW EXECUTABLE SOURCE = PROVENANCE-ATTESTED / CURRENTLY NOT MATERIALIZED.**

This is a documentary/reproducibility gap, not a physical failure.

## Exact next scientific test

### Phase 1 — recover exact source/chart rule

Recover a materializable copy of the pinned Q-B1852 source or a provenance-complete equivalent containing its exact spatial/process chart construction.

### Phase 2 — representative 19D cold test

For one representative first lens `C`:

1. build `K_C`;
2. verify frontier 20 / common-T0 shell 19;
3. generate all 19 legal T0-preserving successor probes;
4. run the same current-Q/Cons/Gram rule;
5. construct candidate `F_C:19x19` and `V_C:80x19`;
6. measure rank before inversion;
7. no pseudoinverse unless an independent Q quotient licenses it;
8. reconstruct the affine/Hessian (`4+6`) source chart from the earned Q/process coordinate rule, not by padding `Apoly`;
9. if chart/rank conditions hold, construct `C_Q^(C)` then `A21(C)`;
10. run b4 and b24 controls.

### Phase 3 — all 16 lenses

Freeze all sixteen conditional blocks before inspecting any cancellation.

### Phase 4 — state-universal operator gate

Test

`M_A21 = sum_C P_C tensor A21(C)`.

- `M_A21=0`: state-universal ensemble cancellation.
- `M_A21!=0`: only then use the actual parent state/FP weights for a state-dependent result.

## Exact resume prompt

> Reprends TDG depuis CURRENT_FRONT_LATEST.md et le handoff `TDG_GATE_B_POST_EVENT_19D_REBASE_HANDOFF_2026-09-16.md`. Ne réutilise pas le shell 16D après une première extension. Cherche d'abord une copie matérialisable du source certifié `qb1852_full_refined.py` SHA256 `ebbe56f7d9c0b19b5c7e688e2b69c7acd2f9a271d161cf558e50e52cad86b5c7`. Si récupéré, exécute un cold test représentatif sur `K_C`: frontière 20, shell T0-préservant 19, puis `F_C`, `V_C`, rang, chart affine/Hessien, `C_Q^(C)`, `A21(C)` en b4/b24. N'utilise ni pseudoinverse, ni padding d'Apoly, ni fit O3/GR. Si le source exact reste introuvable, maintiens SOURCE/REPRO GAP plutôt que d'inventer les équations.

## GR traffic light

🟢 Exact post-event topology closed; false 16D rebase retired.

🟢 Generic current-Q/Cons/Gram post-event evaluation already exists in historical machinery.

🟡 Immediate wall = exact Q-B1852 source/chart recovery and one genuine 19D cold run.

🔴 No O(3), HDA, spin-2 or nonlinear GR closure yet.
