# Complete TDG / Gate A — cos(6theta)=0 / D3 projective-coincidence pass

**Date:** 2026-09-16 (America/Toronto)  
**Status:** **D2-KILLED ANGULAR STRATUM -> D3 = SAME FULL-PIPELINE POSITIVE PROJECTIVE CLASS / STRONG FORMAL-NUMERICAL RATIONAL-SCALE PASS / B4+B24+CUTOFF STABLE / D0,D1 SYMBOLIC ZERO PROOF STILL OPEN**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Parent

Immediate parent:

`TDG_GATE_A_FULL_PIPELINE_GENERIC_VS_PI8_PROJECTIVE_COINCIDENCE_PASS_2026-09-16.md`

commit `b8c4ce3ca721984f75221f392724857ae09bc24f`.

That checkpoint closed the `theta=pi/8` base-stratum singularity: the base leader changes from `G0` to `G1`, but `G1=34.5 G0`, so the full leading positive response class remains unchanged.

This checkpoint tests the independent angular singularity in which the **move-difference leader** itself is killed:

`cos(6theta)=0`.

---

## 1. Representative ray

Choose

`theta=pi/12`.

Then

- `cos(4theta)=1/2 != 0`;
- `cos(5theta)=cos(5pi/12) != 0`;
- `cos(6theta)=0`;
- `cos(7theta)=cos(7pi/12) != 0`.

Thus the base Gram remains on the generic `G0` stratum, while the first robust move-difference coefficient `D2` is removed exactly by the angular factor. The next coefficient `D3` survives.

No base-normal retyping is required for this stratum.

---

## 2. Full-pipeline coefficient matrices

Using the certified Q-B1852/Q-B1850 formal pipeline, define on the generic base normal:

`K_m[T,C] = <nbasis(G0(T)), D_m[C,T]> / ||G0(T)||`.

The generic class is generated by `K2`.

On `cos(6theta)=0`, the first robust surviving move coefficient is `K3`, up to the common nonzero angular scalar `cos(7theta)`.

Direct fresh computation shows `K2` and `K3` are rank 16 and projectively coincident.

---

## 3. Rational-looking proportionality

Across all controls, the best scalar is numerically

`K2 / K3 = 0.0230769230769...`

which agrees with

# `3/130`

within `~3e-13` or better.

Equivalently,

# `K2 ~= (3/130) K3`.

The corresponding positive operators satisfy

`R2=K2^dag K2`, `R3=K3^dag K3`,

with scale

# `R2/R3 ~= (3/130)^2 = 9/16900 = 0.000532544378698...`.

No scalar was chosen from an angular target; the same ratio appears independently across regulator and refinement scheme.

The exact rational identity `3/130` is retained as a strong structural numerical relation, not yet promoted as a symbolic source theorem.

---

## 4. Controls

### b4 a=.04

- `K2/K3` best scalar `0.02307692307694011`;
- difference from `3/130`: `1.70e-14`;
- relative residual using exact `3/130`: `1.00423e-10`;
- positive-operator residual: `1.35030e-10`.

### b4 a=.02

- best scalar `0.023076923076997474`;
- difference from `3/130`: `7.44e-14`;
- `K` residual using `3/130`: `4.00951e-10`;
- `R` residual: `5.39331e-10`.

### b4 a=.01

- best scalar `0.023076923077208027`;
- difference from `3/130`: `2.85e-13`;
- `K` residual: `1.60030e-9`;
- `R` residual: `2.14335e-9`.

The mild worsening at the finest cutoff tracks floating precision as the underlying formal coefficients shrink rapidly.

### b24 a=.02

- best scalar `0.023076923076894264`;
- difference from `3/130`: `-2.88e-14`;
- `K` residual: `4.84987e-10`;
- `R` residual: `6.45611e-10`.

Hence the relation is refinement-scheme stable.

---

## 5. Angular class coverage

The zeros of `cos(6theta)` are

`theta=(2n+1)pi/12`.

At such a ray:

- `cos(4theta)` can never vanish simultaneously, because
  `(2m+1)/12=(2n+1)/8`
  would imply an even integer equals an odd integer after clearing denominators;
- `cos(7theta)` can never vanish simultaneously with `cos(6theta)` by the same parity obstruction.

Therefore every `cos(6theta)=0` ray has:

- generic base leader `G0`;
- killed `D2` move term;
- surviving `D3` move term.

Up to the angular scalar `cos(7theta)`, all such rays are represented by the same `K3` coefficient matrix.

Since `[K3^dag K3]=[K2^dag K2]` to the strong formal-numerical precision above, the complete `cos6theta=0` angular class rejoins the generic positive projective class.

---

## 6. Combined angular picture so far

Assuming the observed structural cancellation `D0=D1=0` is exact, the early full-pipeline angular stratification has only three possibilities:

1. **generic:** `cos4theta !=0`, `cos6theta !=0` -> base `G0`, move `D2`;
2. **base-singular:** `cos4theta=0` -> base `G1`, move `D2`; parent checkpoint proves same projective class via `G1=34.5G0`;
3. **move-singular:** `cos6theta=0` -> base `G0`, move `D3`; this checkpoint proves same projective class via `K2 ~= (3/130)K3`.

The base-singular and move-singular conditions never intersect.

Thus the only remaining obstacle to an all-angle leading-class theorem at these orders is to promote the lower move-coefficient cancellation from floating structural evidence to an exact/source-level statement.

---

## 7. Exact next task

# **D0 / D1 EXACT-CANCELLATION PROOF GATE**

Use the certified carrier-extension functoriality and context-replacement structure:

- Q-B1818G: `I^dag H_K' I = H_K` exactly;
- Q-B1818H: exact one-context -> four-context frontier replacement;
- formal BG: all carriers use the same Q moments `U`, and move dependence enters through frontier selection, Cons, and `avg_back`.

Prove, if possible, that the first two baseline-vs-move formal Gram coefficients descend identically under the exact 1->4 replacement and Cons coequalizer:

`D0=0`, `D1=0`.

Do not promote machine-zero remnants as an algebraic identity without this proof.

---

## 8. Firewalls

- no preferred finite complex `z`;
- no angular averaging;
- no fitted rotation/counterterm;
- no use of GR/O(3) as target;
- rational `3/130` relation is strong structural numerical evidence until symbolically derived;
- Gate-B obstruction remains independently binding;
- O(3), HDA, spin-2 and GR remain unestablished.

---

## 9. GR traffic light

🟢 **Second exceptional angular class closes positively:** killing the generic `D2` term promotes `D3`, but the resulting positive operator is again the same projective class.

🟢 **Strong structural hint:** the invariant ratio is numerically the simple rational `3/130` across b4/b24 and cutoff.

🟡 **Single main seam left for all-angle leading-class closure:** prove `D0=D1=0` exactly rather than at floating floor.

🔴 **No GR closure:** this strengthens analytic-response universality only; it does not derive O(3), HDA, spin-2 or nonlinear GR.