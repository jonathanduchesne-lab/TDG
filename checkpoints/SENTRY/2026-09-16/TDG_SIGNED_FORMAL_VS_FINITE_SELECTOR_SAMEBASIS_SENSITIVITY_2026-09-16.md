# Complete TDG — signed formal vs finite-family selector / same-basis sensitivity audit

**Date:** 2026-09-16 (America/Toronto)  
**Status:** **FORMAL LEADING AND HISTORICAL FINITE-FAMILY SIGNED RAYS ARE EXTREMELY CLOSE IN PHYSICAL 16D MULTIPLICITY EMBEDDING BUT PHYSICALLY DISTINCT BY T3 / SUBLEADING ROTATION IS SMALL IN NORM AND DECISIVE FOR O3-COMPATIBILITY / MULTI-ORDER CANONICALITY REMAINS OPEN**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Why this audit was necessary

The immediate parent

`TDG_SIGNED_ALL_ANGLE_LEADING_SELECTOR_FORMAL_PASS_O3_COMPATIBILITY_FAIL_2026-09-16.md`

commit `bd369a56f9c2d5873656900d7108f98389b0833a`

established that the formal leading signed kernel has nonzero continuum-stable `T3`, while the historical finite-family Q-B1854H selector has `T3=O(a^2)->0`.

However raw 3-component multiplicity coordinates printed by separate eigensolver runs cannot be compared directly because the multiplicity basis `W` lives in a degenerate S4 isotypic sector and may rotate numerically between executions.

This checkpoint performs the comparison in one execution with one common `W` and compares the actual physical 16D vectors `y=W x`.

---

## 1. Same-basis construction

Using the exact recovered Q-B1854 multiplicity construction:

- build one physical Standard3 multiplicity basis `W`;
- apply it to the formal leading coefficient matrix `K2` from Gate A;
- independently evaluate the certified finite-family scalar memory `F(z)` at b4 `a=.02`, `z=2+.7i`;
- perform the same S4 Reynolds symmetrization and skew reduction on both;
- extract the one-dimensional kernel in the same `W`;
- compare physical vectors `y_formal=W x_formal` and `y_finite=W x_finite`;
- evaluate both with the exact historical `irrep -> align -> T3` diagnostic in that same run.

No cross-run coordinate comparison is used.

---

## 2. Result

At b4 `a=.02`:

`|<y_formal,y_finite>| = 0.9999993539504345`.

The corresponding acute angular separation is only about `1.14e-3` radians.

Thus the two signed selector rays are **extremely close in the physical 16D multiplicity embedding**.

Yet their angular isotropy diagnostic is radically different:

- formal leading selector: `T3 = 0.117851134996...`;
- finite-family Q-B1854 selector: `T3 = 0.001392165668...`.

The finite-family value is the already-certified Q-B1854H value that continues to decrease approximately as `O(a^2)` under refinement.

Therefore a very small multiplicity-space rotation is physically decisive for the tetrahedral third-moment obstruction.

---

## 3. Corrected interpretation

The parent checkpoint's statement that the formal leading selector is not the historical O3-compatible selector remains correct in the physical sense: their `T3` invariants differ sharply and one does not satisfy the required continuum isotropy.

But the correct geometric interpretation is now more precise:

# **THE GOOD FINITE-FAMILY SELECTOR IS A SMALL BUT PHYSICALLY DECISIVE SUBLEADING ROTATION OF THE FORMAL LEADING SIGNED RAY.**

It is not a completely unrelated multiplicity copy.

This matters because it strongly suggests that the finite-family selector can plausibly be generated by controlled higher-order analytic corrections to one leading signed line rather than by an unrelated second source.

It does **not** yet prove that Q supplies a unique canonical resummation.

---

## 4. Why a leading-coefficient theorem is insufficient

The signed coefficient audit already shows:

- `S2` and `S3` have the same kernel to machine precision;
- higher formal coefficients begin to move the kernel only very slightly;
- the historical finite-family kernel differs from the leading one by only about `1e-3` radians at b4 `.02`;
- nevertheless the `T3` functional is highly sensitive in precisely this neighborhood.

Hence the physical O3-compatible selection problem is a singularly sensitive multi-order problem:

`small analytic subleading rotation`  
`-> large suppression of forbidden T3`.

A projective or norm-only convergence test is therefore not sufficient to certify the physically correct selector.

---

## 5. Binding consequence

The next gate must distinguish two possibilities:

### Route A — canonical analytic transport/resummation

The full signed coefficient pencil supplies a target-blind, Q-native rule that transports the leading kernel into the finite-family O3-compatible line, with b4/b24 naturality and no preferred finite `z`.

### Route B — readout-family dependence

The good line exists and is numerically stable over several finite readouts, but no Q-native analytic rule selects its particular subleading rotation without effectively choosing a readout or fitted combination.

Only Route A would materially improve the O3 story.

---

## 6. Exact next gate

# **SIGNED COEFFICIENT-PENCIL KERNEL TRANSPORT / CANONICAL RESUMMATION GATE**

1. Build the signed multiplicity coefficient pencil `S(t)=sum_m t^m S_m` directly from the formal Q-B1850 coefficients.
2. Track its one-dimensional kernel as an analytic/projective line wherever `rank S(t)=2`.
3. Derive the line's differential/projective transport from the coefficient pencil itself; do not choose a finite `t=1/z` by hand.
4. Ask whether there is a distinguished Q-native endpoint, fixed line, monodromy-invariant line, asymptotic matching condition, or other already-earned analytic criterion that selects the O3-compatible small rotation.
5. Test the criterion in b4 and b24 and across `.04,.02,.01`.
6. Use `T3`, not only ray overlap, as a required physical discriminator because the current neighborhood is highly sensitive.
7. If a canonical selector is earned, retest the same selected line under `L_Q` and the post-event 19D `A21(C)` family.
8. If not, classify finite-family signed selection as strong empirical naturality without a readout-free physical selection theorem.

---

## 7. Firewalls

- multiplicity coordinates from separate degenerate-eigenspace runs are not physical comparison data;
- physical comparisons must use common `W` or invariant 16D projectors/vectors;
- high ray overlap does not imply small `T3` defect in this singularly sensitive neighborhood;
- no finite `z` chosen by convenience;
- no fitted linear combination of coefficients;
- no O3 projection by hand;
- Gate-B dynamic obstruction remains binding;
- O(3), HDA, spin-2 and GR remain NOT ESTABLISHED.

## 8. GR traffic light

🟢 **Useful sharpening:** the formal and good historical signed rays are extremely close, so the missing structure is localized to a tiny subleading rotation rather than a wholesale change of sector.

🟡 **But the rotation is physically decisive:** `T3` changes by almost two orders of magnitude at `.02` and continues toward zero only for the historical finite-family line.

🟡 **Immediate target:** derive that subleading rotation canonically from the signed coefficient pencil without choosing `z`.

🔴 **No O3/HDA/GR reopening yet:** canonical multi-order signed selection and dynamic co-selection remain unproved.
