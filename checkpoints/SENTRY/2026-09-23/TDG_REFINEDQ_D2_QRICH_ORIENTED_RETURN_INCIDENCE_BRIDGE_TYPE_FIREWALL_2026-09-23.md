# Complete TDG — RefinedQ D2 ↔ Q-rich oriented-return incidence bridge / representation type firewall

**Date:** 2026-09-23 (America/Toronto)  
**Status:** **INCIDENCE-LEVEL BRIDGE = EXACT PASS / CURRENT 101-PARENT CARRIER HAS EXACT ORIENTED CHAIN COMPLEX WITH ker d3 = im d4 (25D) / UNSIGNED FP D2 REPLAY = PASS TO NUMERICAL FLOOR / DIRECT d3^T d3 SUBSTITUTION INTO THE EXISTING 16-SHELL SCALAR REFINEDQ RESPONSE = S4-COVARIANCE FAIL / SAME-SHELL J COEFFICIENT IS THEREFORE NOT TYPED / NO LEGAL CANCELLATION OF c_Q DERIVED / REFINEDQ NO-GO REMAINS BINDING / Q-RICH REMAINS A DISTINCT PROCESS REALIZATION**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Immediate parents

- `checkpoints/SENTRY/2026-09-18/TDG_D2_H3_PATH_CLASS_INTERPARENT_CORE_REDUCTION_2026-09-18.md`
- `checkpoints/SENTRY/2026-09-18/TDG_INTERPARENT_D2_MULTIPLICITY_ALGEBRAIC_NONZERO_NOGO_2026-09-18.md`
- `checkpoints/SENTRY/2026-09-19/TDG_POST_REFINEDQ_NOGO_H5_RICH_PROCESS_EXTENSION_AUDIT_2026-09-19.md`
- historical Q-B1450/Q-B1451/Q-B1453 oriented coherent-return chain.

This checkpoint executes the reconciliation gate without adding a new microscopic law and without fitting O(3).

---

## 1. Exact RefinedQ source replay: the inter-parent factor really is unsigned incidence return

The frozen source identities give, on every parent-face incidence,

`U1_TF = C_f^†`,

and each **other** parent sharing the same B3 face contributes exactly

`3 C_f^†`

to the `P -> F -> P -> F` H3 coefficient, after stripping the common local source scale.

Equivalently, let `U` be the 0/1 B4→B3 incidence matrix on the current coarse carrier. The cross-parent combinatorial factor of the RefinedQ FP source is exactly the off-diagonal part of

`U^T U`,

with the diagonal single-parent return retained separately. In source-normalized form the inter-parent part is proportional to

`3 (U^T U - 4 I)`.

Therefore gate item 3 is closed:

# **CURRENT REFINEDQ FP UNSIGNED INCIDENCE FACTOR = LOCAL RESTRICTION OF THE HISTORICAL U^T U RETURN.**

This is an incidence theorem; it does not yet identify the full RefinedQ response object with Q-rich.

---

## 2. The historical oriented return exists exactly on the actual 101-parent carrier

The actual current coarse carrier reconstructed from the archived Q-B1852 handoff has:

- `101` B4 parent contexts;
- `154` B3 faces.

Construct:

- unsigned incidence `U : C4 -> C3`, entries `0/1`;
- canonical simplicial boundary `d3 : C4 -> C3`, entries `0,±1`, with the same support as `U`.

Exact modular-rank certificates plus exact integer chain multiplication give:

- `rank(U)=101` — unsigned return is full rank;
- `rank(d3)=76`;
- `nullity(d3)=25`.

Independently, the same carrier contains exactly `25` complete B5 cells. Their canonical boundary matrix

`d4 : C5 -> C4`

has

- `rank(d4)=25`;
- `d3 d4 = 0` **exactly**.

By dimension,

`ker(d3) = im(d4)`

exactly on the actual current carrier.

Thus the old six-vertex Q-rich identity was not an artifact of the toy carrier:

# **CURRENT 101-PARENT REFINEDQ CARRIER SUPPORTS THE SAME EXACT ORIENTED BOUNDARY COMPLEX.**

The corresponding return is

`d3^T d3`,

with a 25-dimensional exact B5-boundary kernel, while `U^T U` is full rank.

This materially strengthens the structural bridge.

---

## 3. Fresh replay of the certified FP D2 calculation

Using the archived handoff

`TDG_QB1860BW_RC40F_D2_FULL_HANDOFF_2026-08-26.tar.gz`

SHA256:

`5cfa4c1631dbe9204ddcb6e51f2eac9879f377a0c0f6262aa0204f9c28c794f5`

we reconstructed directly from source:

1. the leading formal parent→B3 response `U1`;
2. only the H3 path class `P -> F -> P -> F`;
3. its cross term with `U1` in the Hermitian response;
4. the D2 Gram coefficient;
5. Cons projection;
6. legal-move child pullback;
7. the 16×16 scalar memory `F_FP` and full response `V_FP`.

Fresh unsigned results at canonical `a=.02`:

- `||F_FP|| = 21.837229101466438`;
- `||V_FP|| = 2.2749332061765997e-8`;
- `rank(F_FP)=16`;
- `rank(V_FP)=16`.

These reproduce the 2026-09-18 checkpoint values.

Projecting `F_FP` on the canonical `(R,A,S)` multiplicity fibre gives the exact archived 3×3 matrix up to one common source scale, with relative residual

`1.20e-14`.

Therefore the fresh replay is validated before any oriented modification.

---

## 4. The tempting direct substitution and why it is forbidden

The incidence-level Q-rich rule suggests replacing the coherent cross-parent kernel

`U^T U -> d3^T d3`.

A process-level diagnostic was therefore performed in the H3 FP contribution only:

- self/single-parent intensity left unchanged;
- every cross-parent return through a shared B3 multiplied by the canonical product of incidence orientations;
- no path deleted;
- no coefficient fitted;
- no O(3) projector or averaging used;
- final RefinedQ readout/Gram/Cons shell left otherwise unchanged.

The resulting matrices remain algebraically nondegenerate:

- `rank(F_or)=16`;
- `rank(V_or)=16`.

But the existing ordinary-S4 shell covariance fails at **O(1)**:

- unsigned replay S4 relative defect: `~5e-14`;
- direct oriented-return substitution S4 relative defect: `~1.40`.

Consequently the oriented matrix no longer lives in the same ordinary-S4 multiplicity commutant in which `(R,A,S)` and the unique normalized `J : l2 -> l1` were derived.

A numerical `c_J` extracted after that substitution would therefore be **ill-typed** and cannot be promoted.

# **DIRECT d3^T d3 INSERTION INTO THE CURRENT SCALAR REFINEDQ D2 SHELL = REPRESENTATION/COVARIANCE FAIL.**

This is not evidence against Q-rich. It shows that Q-rich orientation cannot be imported as a central sign patch while retaining the old scalar RefinedQ response type.

---

## 5. Why the failure is structurally expected

Historical Q-B1397 established that coorientation is a genuine Z2 **tetrad/frame chart** bundle:

- `+q` and `-q` charts have the same Lorentz metric;
- even S4 stays within a chart;
- odd S4 exchanges charts by a proper internal rotation.

Historical Q-B1422–Q-B1428 then built the rich B3 law as an associated-bundle/operator-valued morphism, not as scalar signs attached to the old one-M2 transfer.

Historical Q-B1449 already showed that multiplying old H160 legs by simplicial signs does not create the rich process.

The fresh result above is the exact RefinedQ-D2 analogue of that firewall:

# **THE INCIDENCE KERNEL BRIDGES, BUT THE RESPONSE CODOMAIN DOES NOT BRIDGE BY SIGN SUBSTITUTION.**

To carry `d3^T d3` legally into the current D2 calculation one would first need an explicit natural transformation that lifts the current RefinedQ parent/B3 response into the already-derived cooriented rich-B3 process bundle and then descends its metric response back to the ordinary S4 shell.

No such transformation is presently derived.

Group-averaging the failed scalar insertion is forbidden: it would manufacture dynamics after the fact. Erasing the orientation/which-parent information before recombination is also forbidden by Q-B1453 because it destroys the coherent cross terms and replaces the return by the diagonal degree operator.

---

## 6. Binding reconciliation verdict

### Exact pass

1. The current RefinedQ FP obstruction is genuinely an unsigned parent–B3–parent incidence return.
2. Its incidence factor is exactly the relevant local restriction of historical `U^T U`.
3. The actual 101-parent carrier admits canonical `d3` with the same support.
4. On that actual carrier, `ker d3 = im d4` exactly, dimension 25.
5. Therefore Q-rich acts at exactly the correct topological location and is not an unrelated analogy.

### Exact/type fail

1. `U^T U -> d3^T d3` cannot be inserted directly into the old scalar RefinedQ D2 response while retaining its S4 typing.
2. The ordinary 16-shell covariance defect becomes O(1).
3. Therefore the old `(R,A,S)` inverse and unique `J` coefficient cannot be legally reused on that patched object.
4. A zero obtained after fitting, averaging or manually transporting signs would not be a TDG result.

### Classification

# **REFINEDQ ↔ Q-RICH INCIDENCE BRIDGE = CLOSED POSITIVE.**

# **REFINEDQ ↔ Q-RICH SAME-RESPONSE NATURAL BRIDGE = NOT DERIVED / DIRECT SCALAR VERSION FAILS.**

# **CURRENT REFINEDQ ALGEBRAIC c_Q != 0 NO-GO REMAINS BINDING.**

# **Q-RICH REMAINS A DISTINCT, INDEPENDENTLY MOTIVATED PROCESS REALIZATION WITH ITS OWN B5/PRESYMPLECTIC WALL.**

This is the gate-9 outcome unless a future target-blind coorientation-aware natural transformation is derived from existing Q structure.

---

## 7. Exact next research options

No further work should try another scalar sign choice.

Only two legal directions remain:

1. **Bridge-lift theorem:** derive, from already-earned Q-B1397/Q-B1422–Q-B1428 structure, an explicit coorientation-aware natural transformation from the current RefinedQ response object to the Q-rich B3 bundle, with ordinary metric-S4 descent proved before evaluating `J`.
2. **Keep branches distinct:** retain the RefinedQ D2 no-go as final for that realization and resume the separately motivated Q-rich branch at its genuine later wall: normal/B5/presymplectic activation and the extra scalar mode.

No new H5 operator is admitted by this checkpoint.

---

## 8. Reproducibility

Standalone incidence verifier:

`checkpoints/SENTRY/2026-09-23/verify_refinedq_qrich_incidence_bridge.py`

Expected output:

- B3×B4 = `154×101`;
- complete B5 cells = `25`;
- exact ranks `U,d3,d4 = 101,76,25`;
- `d3*d4 = 0` exact;
- therefore `ker(d3)=im(d4)`.

Verifier SHA256 at checkpoint creation:

`8b8674a3b61d75c94973f353ee07f483c387bc1af805897814b32a6d8a96f331`.

The full D2 FP replay additionally depends on the archived Q-B1852 handoff identified above; it was not rewritten as a new law.

---

## 9. GR traffic light

🟢 **Major structural advance:** the RefinedQ obstruction and Q-rich derivative return are now proven to act on the same parent/B3 incidence skeleton.

🟢 **Stronger than the old toy result:** the current 101-parent carrier itself has exact `ker d3 = im d4`, dimension 25.

🟢 **Replay validated:** the fresh D2 FP reconstruction reproduces the binding 2026-09-18 source/multiplicity result to numerical floor.

🟡 **Bridge is now sharply localized:** the missing object is not incidence; it is the coorientation-aware response-bundle natural transformation.

🔴 **No dynamic-O3 rescue yet:** direct scalar oriented substitution is not covariant and cannot define the old `c_J`.

🔴 **No HDA/spin-2/nonlinear-GR promotion.**
