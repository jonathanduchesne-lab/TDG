# Complete TDG / Root2 — Single factual history vs deterministic successor / initial-selector audit

**Date:** 2026-09-15 (America/Toronto)  
**Status:** **SINGLE FACTUAL HISTORY RETAINED / UNIQUE SUCCESSOR NOT DERIVED FROM CURRENT A+H / INITIAL-SELECTOR COMPLETION COHERENT AS NEW FOUNDATIONAL CANDIDATE / BORN STILL OPEN / NO ROOT2 ADMISSION**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Question

The author-level intuition is now precise:

> an initial impulse launches one factual history; after that, information self-organizes through the web of constraints and a reduction machine continually eliminates incompatible possibilities, while only one factual continuation is retained at each actualization.

The exact question is:

> Does the present recovered TDG already imply that the complete predictive state plus the existing factual history has a **unique legal factual successor** at every step?

This audit is target-blind. No O(3), HDA, spin-2, GR, metric, Born target fitting or microscopic repair is used.

## 1. Firewalls

- Root1 frozen; `A_path` retired.
- global Q uncollapsed.
- factual ledger append-only and separate from predictive/global Q.
- actualisation soustractive retained as Jonathan Duchesne's conceptual contribution; it is not yet a positive winner-selection rule.
- Born NOT DERIVED.
- metric/proper duration NOT DERIVED.
- Lambda OPEN.
- ELGC remains an author-approved working postulate, not an old-Root theorem.
- O(3) FAIL / NOT CLOSED.
- HDA / spin-2 / full nonlinear GR NOT ESTABLISHED.
- no factual-history backflow into global Q.
- no hidden occurrence register is admitted into Root2 by this checkpoint.

## 2. Distinction that must not be blurred

There are two mathematically different statements.

### Single factuality

At each actualization, exactly one event is recorded in the factual history:

`H_n = (B_0, B_1, ..., B_n)`.

This yields one realized history.

### Deterministic unique successor

Before the next actualization, the existing physical state determines one and only one next event:

`B_(n+1) = S(A_n, H_n)`.

Single factuality does **not** imply deterministic unique succession.

A theory can have one actual outcome per event while the pre-event predictive state supports several physically admissible outcomes.

## 3. Current-Q evidence: branch set and weights exist, winner does not

Q-B1819F realizes a Q-native physical extension instrument. For each admissible physical carrier extension `e`, the instrument has a canonical branch operator

`K_e = (1/sqrt(2)) W_e P_(T_e)`

plus a no-extension branch `K_0`, with exact completeness

`K_0^dag K_0 + sum_e K_e^dag K_e = I`.

Conditioned extension probabilities agree exactly with the already-frozen FP relative interface-support weights in the audited scope.

Therefore current TDG already contains:

1. a physically typed set of admissible next extensions;
2. Q-native relative branch weights;
3. exact instrument completeness;
4. same-global-Q multistep propagation.

But Q-B1819G is explicitly only Born equivariance/consistency under the frozen FP/NEF architecture, **not a derivation of Born or of a branch winner**.

Thus the current recovered theory supplies a weighted admissible set, not a general single-valued selector.

## 4. Support theorem

Let the complete current predictive/factual data be `(A_n,H_n)` and let

`E_n = {e : e is a legal next physical extension}`.

Let the current-Q/FP instrument assign weights

`w_n(e) >= 0`.

Define the supported admissible set

`Supp_n = {e in E_n : w_n(e) > 0}`.

If `|Supp_n| > 1`, then `(A_n,H_n)` by itself does not determine a unique next factual event unless an additional selector is already encoded in `(A_n,H_n)`.

The current recovered Root contains no theorem collapsing every physically relevant `Supp_n` to a singleton before factualization.

Therefore:

# **SINGLE FACTUAL HISTORY != UNIQUE SUCCESSOR DERIVED FROM CURRENT A+H.**

This is not a claim that Nature is fundamentally random. It is only a statement about what the present recovered TDG derives.

## 5. Reinterpretation of the author's 'initial impulse'

The initial-impulse idea can be made mathematically coherent without adding a fresh random choice at every event.

Introduce a fixed initial selector state

`lambda_0`.

Let it evolve deterministically by a target-blind rule

`lambda_(n+1) = F(lambda_n, A_n, B_(n+1))`

and let the next factual event be

`B_(n+1) = S(lambda_n, A_n, H_n)`

subject to

`B_(n+1) in Supp_n`.

If `lambda_0`, `F`, and `S` are fixed, the entire factual history is single-valued:

`lambda_0 -> B_0 -> B_1 -> ...`.

This realizes the author's intuition:

> one initial impulse launches a single history; the reduction machine thereafter evolves without needing a new exogenous random draw at each actualization.

## 6. Scientific status of the initial-selector completion

This construction is **coherent as a completion**, but it is **not derived from current Root2**.

Current recovered Q does not supply a certified object identifiable with `lambda_0`, nor a target-blind selector theorem giving `S` and `F`.

Therefore:

**INITIAL SELECTOR STATE (ISS) = NEW FOUNDATIONAL CANDIDATE / ASSUMED IF EXPLORED / NOT ADOPTED.**

Calling it an "initial condition" rather than a "new law" does not remove the need for physical typing: if it affects which factual event occurs, it is physical information and must be specified or derived somewhere.

## 7. Why this is not automatically a forbidden hidden occurrence register

A prohibited hidden occurrence register would simply store factual outcomes and feed them back into global predictive Q as an extra hidden memory field.

The cleaner ISS candidate is different in intent:

- `lambda_0` is fixed before the factual sequence;
- its evolution is prospective, not a copy of the ledger;
- it may select only among already-Q-licensed branches;
- it must not overwrite or modify global predictive Q except through the ordinary legal actualization/update map;
- the factual ledger remains append-only and separate.

However this distinction is only a proposed typing firewall. A future ISS model must prove it rather than assume it verbally.

If `lambda` degenerates into a covert record of past outcomes or a prewritten lookup table for the entire future, it fails the intended minimality/physicality test.

## 8. Born moves rather than disappears

A deterministic selector does not derive Born automatically.

If repeated preparations share the same predictive `A` but differ in selector microstate `lambda`, then observed frequencies require a measure/distribution

`mu(lambda_0 | preparation)`

such that

`mu{lambda_0 : S(lambda_0,A,H)=e}`

reproduces the retained FP/NEF/Born-compatible branch weights.

Equivalently, in a one-universe fixed-seed picture, one would need a rigorous typicality/ergodic theorem showing that repeated subsystems sampled along the unique history exhibit those frequencies.

Neither result is currently derived.

Hence:

- single-history determinism is logically possible;
- Born can be retyped as a measure/typicality problem over the initial selector state;
- **Born remains NOT DERIVED**.

## 9. Strongest present conclusion

The current corpus supports the following three-layer picture:

1. **Predictive layer:** Q/FP/NEF supplies several possible legal continuations with physical weights.
2. **Factual layer:** one and only one continuation is recorded as the realized history.
3. **Selection layer:** the recovered Root does not yet derive the map from the weighted predictive set to the unique factual continuation.

The author's "initial impulse" is therefore not contradicted. It identifies exactly one possible location for the missing selection information: a once-fixed initial selector condition rather than an independent random decision at every event.

But this remains a new foundational candidate until Q itself derives such a selector or external evidence motivates it.

## 10. Clean target-blind admission tests for ISS

Any future ISS candidate must pass, before any GR comparison:

1. **Physical typing:** identify `lambda` from an independent pretarget physical principle or explicitly declare it new foundational content.
2. **Single-valuedness:** for every tested state, `S(lambda,A,H)` yields exactly one supported legal event.
3. **No branch creation:** ISS may select among Q-licensed supported branches but may not create an otherwise forbidden event.
4. **Covariance/relabeling:** physical relabelings cannot change factual predictions merely by notation.
5. **No ledger backflow:** the selector cannot use an illicit hidden copy of factual history to alter global Q.
6. **Composition:** sequential selection must be well-defined under legal event composition.
7. **Born/FP compatibility:** ensemble or typicality frequencies must reproduce retained branch weights without fitting each experiment separately.
8. **Non-script test:** `lambda_0` must have a compact physical law/state interpretation; a literal encoding of the complete future history is not explanatory completion.
9. **Intervention robustness:** changing a physically licensed intervention must alter the selector only through licensed physical inputs, not through target knowledge.
10. **No downstream tuning:** no O(3), HDA, spin-2 or GR result may be used to define `S`, `F`, or `mu`.

## 11. Relation to A+B->C theorem

The recent quotient-descent theorem remains intact.

Once `B_(n+1)` has become factual, the complete predictive state `A_n` plus the fully typed event `B_(n+1)` determines the post-event predictive class `C` under composition-closed interface semantics.

Therefore the remaining conceptual asymmetry is sharp:

`A + B -> C` = abstractly closed at predictive-quotient level;

`A -> B` = selection not closed by current Root.

ISS is one possible completion of the second arrow only.

## 12. DERIVED / CANDIDATE / OPEN / NEXT

### DERIVED / RETAINED

- one factual ledger/history is compatible with an uncollapsed multi-possibility predictive Q;
- Q-B1819F provides physically typed extension branches and relative weights;
- Q-B1819G is Born equivariance/consistency, not winner selection;
- current `A+H` does not generally imply a derived singleton supported branch;
- therefore single factual history does not establish deterministic successor from current Root data.

### CANDIDATE / NOT ADOPTED

- fixed initial selector state `lambda_0` plus target-blind deterministic selector/update `(S,F)`;
- interpretation of the initial impulse as the one-time selector datum launching the factual history.

### OPEN

- Q-native derivation or physical source of `lambda_0`;
- selector law `S` and update `F`;
- whether a compact selector can reproduce all required quantum correlations/contexts;
- Born measure or typicality derivation;
- full quotient-descent audit of emergent continuum structures;
- metric duration, Lambda, O(3), HDA, spin-2 and nonlinear GR.

### NEXT

Before inventing `S`, test whether any already-derived current-Q quantity can serve as a target-blind selector state **without** being merely the predictive branch weights themselves, a factual-history register, or a downstream repair variable. If none exists, freeze ISS as an explicit new foundational option rather than pretending it is derived.

## 13. GR traffic light

🟢 **Advance:** the conceptual problem is sharply localized. The missing object is no longer vaguely "collapse"; it is the map from a Q-weighted admissible continuation set to one factual successor.

🟢 **Author intuition survives:** a one-time initial impulse can coherently serve as the source of a single deterministic factual history in a future completion.

🟡 **But not yet derived:** the present Root has branch weights, not a winner selector. The initial selector state and its measure/law remain open.

🔴 **No GR promotion:** Born remains not derived; no O(3), HDA, spin-2 or full nonlinear GR result follows from this checkpoint.
