# Complete TDG / Root2 — l=1/l=2 predictive-quotient irreducibility dichotomy

**Date:** 2026-09-15 (America/Toronto)  
**Status:** **EXACT QUOTIENT-SYMMETRY DICHOTOMY / NO PARTIAL NULL-SECTOR RESCUE OF SO(3) / L1 PROCESS SECTOR STRONGLY PHYSICAL IN DECLARED INTERFACE / L2 SOURCE SECTOR EITHER FULLY SURVIVES A PHYSICAL SO(3) QUOTIENT OR SO(3) ITSELF FAILS TO DESCEND / A21 OBSTRUCTION SHARPLY STRENGTHENED / NO NEW ROOT2 LAW**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Scope

This checkpoint executes the final conservative audit announced by the A21 predictive-null/enlarged-covariance gate.

Question:

> Are the Q-derived `l=1` and `l=2` sectors used in the retained nonzero `A_21` response genuine predictive-quotient structures, or could one partially quotient away the troublesome source/target directions while keeping the derived `SO(3)` as a physical symmetry?

No new postulate, order parameter, microscopic transport law, preferred readout, projector or GR target is introduced.

The initial impulse `I0` remains a given boundary condition and is irrelevant here.

## 1. Binding recovered facts

The retained corpus gives:

- a Q-native future-timelike Herm2 causal germ;
- a Q-native little group `SU(2)/{+-I} -> SO(3)`;
- canonical Q-Casimir `l=0,1,2` projectors;
- a physical/process spatial Standard3, identified as Q-Casimir `l=1`;
- a traceless-Hessian/source module identified as Q-Casimir `l=2`, dimension 5;
- a retained Q-B1852 cross response `A_21 : l=2 -> l=1`;
- `A_21` remains nonzero in the b4/b24 continuum family with extrapolations
  - b4 `0.1424778761`,
  - b24 `0.1424778616`,
  - relative difference about `1.02e-7`;
- exact same-final Cons/path and disconnected-spectator controls do not remove the structural response;
- Q-B1857/Q-B1858 remove known raw multiplicity/scale artifacts without removing `A_21`;
- current Q supplies no finite physical continuum `l=3` order parameter completing the response covariantly.

Q-B1849/Q-B1851 additionally give strong interface-relative physical typing of the process `1+3` descriptor and process Standard3: actual legal-move origin, no-fit construction, exact 120/120 same-final path independence in the tested shell.

## 2. Predictive-null subspaces

Let

`V_2 ~= l=2`

be the Q-defined linearized source/intervention module and

`V_1 ~= l=1`

be the Q-defined physical/process response module at the declared interface.

Define predictive-null subspaces:

- `N_2 subset V_2`: source directions that change no licensed future/process response at the declared interface;
- `N_1 subset V_1`: response directions that are physically null under all licensed downstream readouts/continuations at that interface.

The physical quotient sectors, if they exist, are

`Vbar_i = V_i / N_i`.

A universal provenance-complete Q-B79 dataset would be needed to calculate `N_i` explicitly in full generality. The theorem below does not require such a calculation.

## 3. Symmetry-descent lemma

Let a group `G` act linearly on `V` through `D(g)`.

The action descends to the quotient `V/N` iff `N` is `G`-invariant:

`D(g) N subset N` for every `g in G`.

Reason: the value of `D(g)[v]` must be independent of the representative `v` of a quotient class.

This is exact linear algebra.

## 4. Irreducibility theorem

For connected `SO(3)`, the real irreducible modules `l=1` and `l=2` are irreducible.

Therefore every `SO(3)`-invariant linear subspace of either `V_1` or `V_2` is either

`{0}`

or the whole module.

Consequently, if the Q-derived `SO(3)` action is to descend as a physical symmetry on the predictive quotient, then

`N_i = 0` or `N_i = V_i`.

There is **no proper nonzero predictive-null subspace compatible with retaining the full SO(3) action on an irreducible sector.**

## 5. Nonzero A21 excludes total nullness

The retained response is nonzero:

`A_21 != 0`.

In the declared audited interface/refinement family, available controls favor response-visible status rather than descriptive nullness.

Thus at least one `V_2` direction produces a nonzero retained `V_1` response. Therefore the source sector cannot be wholly predictive-null in that scope:

`N_2 != V_2`.

The target process Standard3 is independently strongly physical/interface-relative from Q-B1849/Q-B1851, so the target sector is not wholly null either:

`N_1 != V_1`.

Hence, **if the Q-derived SO(3) acts physically on the predictive quotient**, irreducibility forces

`N_2 = 0`,

`N_1 = 0`.

So both full irreducible sectors survive.

## 6. Exact no-partial-quotient-rescue theorem

Combining Sections 3-5:

# **A PARTIAL QUOTIENT CANNOT REMOVE ONLY THE TROUBLESOME l=2/l=1 DIRECTIONS WHILE PRESERVING THE FULL Q-DERIVED SO(3) ACTION.**

There are only two structural possibilities:

### Case S — SO(3) descends physically

Then the full `l=2` and `l=1` sectors survive the quotient in the declared scope.

A covariant linear response between them would have to be an SO(3) intertwiner.

Since `l=2` and `l=1` are inequivalent irreducibles,

`Hom_SO(3)(l=2,l=1) = {0}`.

Equivalently the Casimir defect is

`C_out A_21 - A_21 C_in = -4 A_21`.

Thus a physical nonzero `A_21` cannot coexist with unbroken SO(3)-covariance of this linear constitutive response unless additional independently physical transforming data are present.

### Case Q — a proper predictive quotient removes some sector directions

Then the required null subspace is not SO(3)-invariant, because `l=1` and `l=2` are irreducible.

Therefore the full SO(3) action itself does **not** descend to that physical quotient.

In that case the quotient has already selected/broken the symmetry before one even asks whether `A_21` is an intertwiner.

## 7. Strong dichotomy

The attempted benign escape

> “perhaps only a 3D physical piece of the 5D l=2 survives, and then the cross block is harmless while SO(3) remains physical”

is mathematically unavailable.

A rank-3 response such as the tetrahedral `T2 -> Standard3` component may certainly identify a 3D response-visible subspace at a fixed presentation/context. But that 3D subspace is not a proper SO(3)-invariant subrepresentation of `l=2`.

Therefore it can only be interpreted as:

- evidence of symmetry-selected/contextual structure, or
- incomplete information about a fully physical `l=2` sector,

not as an SO(3)-preserving quotient of `l=2`.

## 8. Operational version — source need not be a state variable

This theorem does not require interpreting the `l=2` Hessian/source as an autonomous physical state.

It is sufficient that:

- `V_2` is a physically licensed intervention/source module;
- `V_1` is a physically licensed process-response module;
- SO(3) is claimed as a physical symmetry of the intervention-response experiment.

Then covariance of the linear susceptibility still requires an intertwiner between the source and response representations unless additional transforming background/order data are present.

Thus the sector-physicality issue cannot be evaded merely by calling `l=2` a "probe" rather than a "state".

## 9. Relation to the finite l=3 completion

A transforming `l=3` order parameter could in principle make the constitutive **law** covariant while a fixed state breaks SO(3) to A4.

That representation-level construction is exact.

But the current regular-Q continuum audit remains negative for the physical completion:

- Q-selected angular `T3 ~ O(a^2) -> 0`;
- no finite Q-native `l=3` statehood/transport/co-selection;
- dynamic co-selection defect persists.

Therefore current Q does not presently supply the additional transforming data required to evade Case S.

## 10. Consequence for the old foundational choice gate

The 2026-09-11 choice gate correctly said that kinematic SO(3) alone does not force dynamical SO(3).

The present theorem does **not** add that missing physical principle.

What it closes is narrower and exact:

- one cannot preserve physical SO(3) by a selective predictive quotient of the `l=1/l=2` sectors;
- if physical SO(3) is retained, the sectors survive fully and the nonzero cross block is genuinely non-intertwining;
- if a partial quotient is used, SO(3) itself fails to descend.

Thus the remaining choice is no longer about sector bookkeeping. It is genuinely about the physics of the emergent symmetry.

## 11. ELGC sandbox consequence — ASSUMED, NOT DERIVED

The repository contains the author-approved but quarantined working postulate ELGC:

> if Q derives a regular-phase physical little group and no physical transforming order parameter survives, the universal effective constitutive law must be little-group covariant.

Under **ELGC as an explicit assumption**, the present theorem strengthens the status of the current regular-phase realization:

- quotienting cannot rescue it;
- the finite `l=3` completion is not derived;
- nonzero `A_21` is therefore a clean pretarget failure of the present constitutive realization under ELGC.

This establishes a **generic completion necessity under ELGC**, but does not select or admit any concrete new microscopic law. Every concrete hypothesis still requires the frozen G0-G10 protocol and independent parameter fixing.

## 12. Assistant intuition — quarantined

**ASSISTANT INTUITION, NOT DERIVED:** this dichotomy suggests that the physically relevant next object is not a full microscopic connection matrix but the mechanism by which the regular-phase predictive interface acquires or fails to acquire its little-group symmetry.

In other words, the problem may be closer to a universality-class/phase-selection problem than to a missing transport coefficient.

This is not Jonathan-origin and is not adopted as a theorem.

## 13. Exact next gate

No further quotient bookkeeping can remove the current obstruction while preserving full SO(3).

The legal next research fork is therefore:

1. **ELGC sandbox route:** treat the author-approved ELGC postulate as an explicit pretarget assumption and search for the smallest coefficient-free/independently-fixed Q modification whose regular continuum response satisfies it, under G0-G10;
2. **non-GR phenomenology route:** retain `A_21` as physical symmetry breaking and derive its observable consequences;
3. **new primary-source route:** recover missing Q-B79/full multi-refinement data or other primary Q material that changes the response classification upstream.

Do not invent a microscopic `J`, hand-delete `l=2`, or fit a counterterm.

## 14. GR traffic light

🟢 **Major conceptual closure:** partial predictive quotient is not a legal SO(3)-preserving escape.

🟢 **Sector audit is substantially closed:** if SO(3) descends physically, full `l=1` and `l=2` irreps survive in the audited interface.

🟡 **Under author-approved ELGC, current regular-phase realization has a clean pretarget covariance failure.**

🟡 **Concrete completion still unselected and must pass G0-G10.**

🔴 **O(3), HDA, spin-2 and full nonlinear GR remain unestablished.**
