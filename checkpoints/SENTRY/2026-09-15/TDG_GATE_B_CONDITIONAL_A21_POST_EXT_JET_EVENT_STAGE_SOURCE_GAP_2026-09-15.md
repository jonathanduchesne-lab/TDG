# Complete TDG / History Ensemble Gate B — conditional A21 post-extension jet / event-stage source gap

**Date:** 2026-09-15 (America/Toronto)  
**Status:** **16-LENS DOMAIN + FP MEASURE + OPERATOR-MOMENT TEST RETAINED / CONDITIONAL `A21(C)` FAMILY NOT YET CONSTRUCTED / BASE ONE-MOVE JET CANNOT BE COLUMN-AVERAGED / POST-EXTENSION REBASE REQUIRES EVENT-STAGE TYPING / CURRENT CORPUS INSUFFICIENT FOR GATE-B NUMERICAL EXECUTION**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Purpose

Gate B now has:

- an exact physical 16-lens domain: the complete `T0`-preserving first-extension outcomes from `Kstar`;
- an exact physical conditioned FP measure functional
  `mu_T0(C|rho)=Tr(rho P_C)/Tr(rho B_T0)`;
- an exact state-universal operator-moment formulation of ensemble covariance;
- licensed `avg_back/Cons` return transport for move-conditioned scalar responses.

The remaining object required by the breathing-test ensemble theorem is a genuine family

`A21(C): l=2 -> l=1`, `C in others`,

where each member is the continuum/cross-Casimir one-move jet **conditioned on the lens/event C** and read on a common licensed interface.

This checkpoint audits whether that family already exists or can be inferred from existing Q-B1850/Q-B1852 data without new choices.

Answer: **not yet**.

---

## 1. Do not confuse Q-B1850 `A_ij` with continuum `A21`

Q-B1850 defines the 16x16 scalar cross-memory response

`A_ij = Re <N_i, G_i^(j)-G_i>_F / ||G_i||_F`,

with rows = base shell contexts and columns = legal first moves.

This is a move-resolved predictive-memory scalar transport object.

The modern symmetry obstruction is instead

`A21 = P_l1 C_Q P_l2`,

where the one-move jet is

`C_Q = J_aff^-1 J_H`,

with affine output `1+3` and Hessian input `Sym2(3)=1+5`.

The two objects are related through the larger Q-B1850/51/52 construction, but they are not equal and one is not a column slice of the other.

Therefore averaging Q-B1850 columns cannot be advertised as averaging the modern `A21` obstruction.

---

## 2. Exact current one-move jet pipeline

Recovered P10J/P10K source audits give the complete rebuilt chain

`H moments -> determinant synchronization -> raw occurrences -> Cons`

`-> G,dG -> exact rcoord -> F,V`

`-> L = Vcentral F^-1`

`-> J_aff = L Aff, J_H = L Hess`

`-> C_Q = J_aff^-1 J_H`.

The exact `rcoord` identity applies to every one of the 17 contexts and all 16 legal moves, producing the full `68x16` vector-response matrix `V`.

Thus the current base `C_Q` is a **global shell jet assembled from the complete 16-move response family around the current cut**, not sixteen independent `C_Q` matrices indexed by first outcome.

The modern

`A21=P_l1 C_Q P_l2`

is therefore one base-cut aggregate one-move jet obstruction.

---

## 3. What a genuine conditional family would require

For each first outcome `C in others`, define the post-extension carrier

`K_C = Kstar + [moves[C]]`.

Because `T0` survives these sixteen outcomes, a conditional Gate-B jet is conceptually well typed only if the same Q pipeline can be rebuilt on the **post-C predictive cut**:

1. construct the legal post-C current shell around the retained common interface `T0`;
2. enumerate its complete next-move family from current Q/Ext, not by reusing base labels by hand;
3. compute post-C `H` moments and Cons classes;
4. build post-C `G^(C), dG^(C)`;
5. build `F^(C), V^(C)` and central prolongation `L^(C)`;
6. freeze the same affine/Hessian source families on that cut;
7. construct
   `C_Q^(C) = (J_aff^(C))^-1 J_H^(C)`;
8. transport the Q-derived Casimir/little-group typing to the common comparison interface by already-earned transport only;
9. define
   `A21(C)=P_l1^(C) C_Q^(C) P_l2^(C)`
   in the common licensed representation convention.

Only then is the FP ensemble mean of `A21(C)` physically meaningful.

---

## 4. Why the 120 Q-B1850 Cons diamonds do not already supply this family

Q-B1850 exactly tested all `C(16,2)=120` distinct legal move pairs and found same-final path/order independence:

`response(C then D) = response(D then C)`

for the tested final local Gram responses.

This is an exact and important confluence theorem.

But it does **not** reconstruct the complete post-C shell jet required above:

- it tests same-final distinct-pair final responses, not a full post-C basis of legal affine/Hessian fields;
- it does not by itself supply all repeated/same-branch continuations;
- it does not construct one invertible `F^(C)` and `V^(C)` for every post-C cut;
- it does not form sixteen post-C `J_aff^(C), J_H^(C), C_Q^(C)` matrices;
- therefore it does not yield sixteen continuum `A21(C)` blocks.

Same-final confluence is necessary compatibility data, not a substitute for conditional jet reconstruction.

---

## 5. Event-stage firewall blocks a silent poststate choice

Recovered GR62/P10 lineage proves that current Q distinguishes at least two natural process stages:

`interface occurrence != completed cell-extension occurrence`.

Their one-step effect shapes remain different under refinement.

The historical audit explicitly classifies

`unique event placement = NOT DERIVED`.

It also shows that a naive local repeated-port history is not the legal whole-cell cut history: after one legal move, other moves remain immediately available on the still-active boundary, and a single local post-event state cannot stand in for the entire cut.

Therefore a conditional `A21(C)` construction may not silently choose:

- B3-interface occurrence;
- fresh-B4 completed extension;
- full active B4+B3 post-cut state;

as the unique physical lens stage merely because one gives a convenient answer.

The stage/interface used for the ensemble member must be independently fixed by existing Q/FP/actualization typing or explicitly retained as a conditional family of stage hypotheses.

---

## 6. Consequence for the state-universal operator-moment theorem

The previous exact theorem remains valid:

`M_bad = sum_C P_C tensor Pi_bad(A21(C))`.

State-universal ensemble covariance is equivalent to `M_bad=0` on the conditioned support.

But the theorem cannot yet be numerically or algebraically evaluated because the current corpus does not provide the required sixteen `A21(C)` members.

This is now the first Gate-B source/typing gap.

The unrecovered numerical `rho_Kstar` remains secondary: it is needed only after a nonzero `M_bad` is found and a state-dependent mean is desired.

---

## 7. Exact new Gate-B frontier

### CONDITIONAL POST-EXTENSION ONE-MOVE JET / EVENT-STAGE GATE

Before any ensemble average:

1. freeze the physical event stage at which the actualization lens response is to be evaluated, using only existing Root/Q/FP typing;
2. for each `C in others`, rebuild the complete post-C legal shell at common `T0`;
3. reconstruct the full `F^(C),V^(C),L^(C),J_aff^(C),J_H^(C),C_Q^(C)` pipeline;
4. verify invertibility/rank and Cons/path/spectator locality without GR fitting;
5. transport the post-C Casimir sectors to a common comparison convention;
6. define all sixteen `A21(C)`;
7. only then build the exact FP operator moment and test state-universal cancellation/covariance.

If the current source artifacts cannot support this reconstruction, record the family as **NOT YET DERIVED**, not as zero and not as a Gate-B failure.

---

## 8. DERIVED / OPEN

### DERIVED / RETAINED

- physical 16-lens common-`T0` domain;
- exact conditioned FP measure functional;
- exact operator-moment retype;
- Q-B1850 move-resolved scalar cross-memory map;
- Q-B1851 full `68x16` `1+3` prolongation;
- base Q-B1852/Q-B1858 one-move jet `C_Q` and base `A21` obstruction;
- exact 120/120 same-final Cons confluence on tested distinct pairs;
- event-stage distinction is real in current Q;
- unique event placement is NOT DERIVED.

### NOT DERIVED / OPEN

- sixteen post-C continuum one-move jets `C_Q^(C)`;
- sixteen conditional `A21(C)` blocks;
- a unique physical event stage for that conditional ensemble response;
- Gate-B `M_bad` value;
- FP-weighted state-dependent mean/variance.

---

## 9. GR traffic light

🟢 **Major clarification:** Gate B is not blocked by measure selection anymore.

🟢 **No false averaging:** base `A21` and Q-B1850 `A_ij` are correctly kept distinct.

🟢 **Exact construction target:** the missing object is now a finite post-extension conditional jet family.

🟡 **Immediate bottleneck:** event-stage typing + sixteen post-C shell rebuilds.

🔴 **No GR promotion:** ensemble cancellation has not been tested because the conditional `A21(C)` family does not yet exist in the recovered corpus; O(3), HDA, spin-2 and nonlinear GR remain unestablished.
