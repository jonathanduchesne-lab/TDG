# TDG Route B / B3 — local higher-cell finite-memory projectability no-go

Date: 2026-09-15 (America/Toronto)
Status: LATEST-WINS SENTRY CHECKPOINT / Route B Stage-I refinement
Historical FINAL-CERTIFIED authority: Q-B1858L unchanged.

## Scope

This checkpoint tests a narrower B3 inference:

> Does local higher-order/history growth or the need for ordered-triple tomography force a new primitive higher-cell memory carrier?

Answer in the tested scope: **NO.**

The surviving open B3 question is stricter: whether the intervention-complete minimal predictive quotient of the actual Q must grow or change type under genuine physical refinement in a way that cannot be carried by one fixed licensed finite predictive memory.

## Binding historical evidence

### Q-B1001
Three-step selected-future tomography fixes the third conditional branch from the selected three-step composite and the selected ordered-pair composite:

`M_{h|fg} = P_{hgf} N_{gf}^{-1}`

provided the ordered-pair occurrence register is physically readable and the maps are invertible on the retained predictive factor.

This shows ordered-triple response information is tomographically identifiable when the relevant process state is typed, but it does not by itself imply a new primitive higher-cell degree of freedom.

### Q-B1003
At a declared common Cons endpoint, hidden future-visible order memory is a projectability/type failure unless it is explicitly retained as physical state. Nontrivial frame 2-morphisms may remain and carry curvature.

### Q-B1004
The outgoing candidate law can depend only on the carrier subset `S` through

`Phi_h(S) = sum_{s in S} Phi(s,h)`.

This exactly reproduces the previously screened pair law, is independent of the order used to reach `S`, introduces no ordered-triple coefficient/associator, and gives the tested 3-cell package including `tau = O(a^3)` and asymptotic second-Bianchi behavior.

### Q-B1005
The same subset law extends through all subsets of four local directions. All 24 execution orders are generated from one edge law with no new ordered-quadruple coefficient. This is a direct counterexample to the inference that every higher local cell requires a new primitive k-body polar.

### Q-B1016
The subset-projectable C/K edge law has an exact uniformly-gapped Hermitian Schur/Feshbach realization. For a carrier subset `S`, independent fast channels give

`Sigma_{S,h} = a^2 sum_{s in S} Phi(s,h)`

with no order history. In the locked control, the effective-core mismatch is below 1.1e-16, full edge-response mismatch below 3.5e-15, and the dilated regional model retains `tau ~ a^3` and `||K-I|| ~ a^2`.

This upgrades the subset-projectable law from a purely formal effective rule to an exact same-regional finite Hermitian constructive realization.

Firewall retained: choosing that mediator architecture as the physical `Q_loc` remains model-class construction, not a theorem uniquely derived from frozen Root1.

## Fresh independent verifier

File:

`checkpoints/SENTRY/2026-09-15/verify_b3_subset_projectability_finite_memory.py`

The verifier uses a generic vector-valued primitive pair-response table and no GR/O3/HDA target. It enumerates all ordered prefixes of the four-direction Boolean cell.

Ordered-history counts by depth:

`[1, 4, 12, 24, 24]`

Carrier subset-state counts:

`[1, 4, 6, 4, 1]`

Total distinct carrier subset states: `16`.

For histories with the same subset `S`, all future candidate laws are exactly equal. A nontrivial carrier relabel gives covariance defect at floating floor (~1.7e-16).

Verifier status: **5/5 PASS.**

## Exact reclassification

The following inference is now closed:

`combinatorial history growth / local higher-step distinguishability / fresh local jets`

`=> primitive higher-cell memory carrier must grow`

This implication is false even inside TDG-like branch-aware process architecture. A finite projectable carrier can encode all local ordered histories through a finite subset state while keeping higher response curvature in frame/process morphisms.

Likewise, the earlier GR64 fresh-carrier `+5` pattern and Q-B1381 local `6 -> 12 -> 17 -> ...` pattern cannot be promoted into primitive-memory growth evidence without an intervention-complete minimal predictive quotient calculation.

## What remains genuinely open

B3 is **not globally closed** because the constructive projectable law is not yet uniquely selected by frozen Root1/current physical Q.

The only admissible positive B3 witness now is:

1. choose genuine physical refinements of the same already-licensed Q/Ext/Cons architecture;
2. freeze separating preparation and future-intervention families;
3. construct the full relational cut-Hankel `H_Sigma(f,p)=Resp_Q(f o p)` before reduced descriptive quotients;
4. compute the right-null quotient/minimal predictive space at each refinement;
5. construct refinement intertwiners between those minimal spaces;
6. test all licensed fixed finite predictive-memory realizations and spectator/dilation/null controls;
7. only unavoidable refinement-natural growth/type obstruction counts as a G4/G6 witness.

No local history-count, higher-cell-count, fresh-fibre-count, susceptibility rank or reduced Gram rank may substitute for this test.

## Route B Stage-I effect

- G0a globalization incompleteness: PASS retained.
- G0b necessity of a nonzero primitive higher-cell variable: NOT DERIVED.
- G2 finite/local identifiability: FAIL-TO-PASS strengthened.
- G4 local higher-cell necessity: FAIL in the projectable finite-memory constructive class.
- G6 local higher-order response margin over fixed finite memory: FAIL in the projectable finite-memory constructive class.
- G8 global refinement survival/minimal predictive growth: OPEN and now decisive.

## GR traffic light

🟢 Local higher-cell/process hierarchy can close on a finite projectable carrier; Q-B1016 supplies an exact gapped Hermitian realization.

🟡 Only global intervention-complete cut-Hankel growth under genuine physical refinement can still rescue Route B.

🔴 No new higher-cell coefficient, primitive memory carrier, O3, HDA, spin-2 or nonlinear GR claim is licensed by this checkpoint.
