# TDG Route B / G8 — historical observability audit remains open

Date: 2026-09-15 (America/Toronto)
Status: LATEST-WINS SENTRY AUDIT / NO HIDDEN HISTORICAL CLOSURE FOUND
Historical FINAL-CERTIFIED authority: Q-B1858L unchanged.

## Question

After closing local B3 higher-cell/history growth as an identifying witness, did the later TDG corpus already perform the only remaining legal test under another name: intervention-complete minimal predictive quotient / observability saturation under genuine refinement of the physical geometric Q?

## Answer

**No positive current-Q closure was recovered.**

The corpus repeatedly defines the correct gate, supplies exact controls and finite-memory adversaries, but continues to mark the physical geometric observability saturation as OPEN.

## Binding historical evidence

### Q-B79
The legal predictive invariant is the relational cut-Hankel

`H_Sigma(f,p)=Resp_Q(f o p)`

with right-null quotient as the canonical minimal predictive space. A quantum memory of Hilbert dimension `d` obeys `rank(H)<=d^2`.

### PA observability reformulation

The EffG/PA line reformulates the same concept as the Q-observability codistribution generated by all admissible iterated responses of intrinsic metric readouts `h`.

On a regular patch,

`dim P_G^pred = r_obs`.

If six independent h and six first-normal-response K are independent and the full hierarchy stabilizes at `r_obs=12`, higher metric responses are functionally dependent on `(h,K)`.

This is a useful equivalent geometric diagnostic, not a replacement for intervention completeness.

### Six-qubit control

A six-qubit product/coadjoint witness has exact `r_obs=12` because each qubit contributes two independent response directions. Generic entangling terms increase `r_obs`.

Classification for present purposes: **EXACT CONTROL / NOT CURRENT-Q GEOMETRIC DERIVATION**.

The same historical recovery explicitly states that a genuinely tetrad-bearing Q observability saturation still had to be computed; the six-qubit witness was insufficient.

### Q-B1204
The H160 line explicitly classifies actual `(h,K)` predictive closure as OPEN and forbids relabeling susceptibility/control rank as state observability rank. It requires:

1. h and K as state/readout functions of the same Q predictive state;
2. `rank d(h,K)=12`;
3. full intervention-complete observability codistribution;
4. saturation `r_obs=12`;
5. explicit testing of phase/history/occurrence/hidden-correlation sectors.

### Q-B1332→1382
A fixed-effect H160/BSR class first shows extra predictive closure (26D for q>0). After fresh-archive/Cons typing the global Heisenberg family closes `21 -> 42 -> 42 -> ...`, while local one-face response grows beyond12 but is reconstructible from neighboring global h germs.

This is a strong finite-memory/process adversary against primitive-memory inference, but it is not the physical multi-refinement cut-Hankel saturation of the frozen current Q.

### Q-B1642
A much later recovery still lists, before physical HDA:

- true Q state/readouts h,K;
- `r_obs=12` full-future saturation;
- stable open-background/refinement family;
- hidden phase/history/correlation search.

The explicit stop rule says not to reopen FS/HDA physically before these gates.

Therefore the required G8 test was still OPEN at Q-B1642. No later source recovered in this audit supersedes that with a provenance-complete current-Q multi-refinement minimal predictive quotient.

## Consequence for Route B

There is no hidden historical positive G8 witness to rescue primitive higher-cell microphysics.

The latest Route-B state is therefore:

- local finite higher-cell/history hierarchy: **NON-IDENTIFYING / CLOSED AS PRIMITIVE-MEMORY ARGUMENT**;
- exact finite projectable Hermitian realization: **CONSTRUCTIVE PASS**;
- six-qubit / S3 / M2 Hankel controls: **CONTROL PASSES ONLY**;
- actual-Q geometric full-future minimal predictive quotient across genuine refinements: **OPEN / NOT RECOVERED**;
- primitive nonzero higher-cell necessity: **NOT DERIVED**.

## Only admissible next positive test

A future positive Route-B result must calculate, from the same frozen/licensed physical Q on at least two genuine refinements:

1. target-blind separating preparations and future interventions;
2. full response matrices before descriptive quotient loss;
3. Q-B79 right-null minimal predictive spaces;
4. their ranks and operator-system types;
5. Q-native refinement intertwiners;
6. a proof that no fixed licensed finite predictive carrier realizes all refinements naturally.

Only unavoidable growth/type obstruction of the **minimal intervention-complete quotient itself** can count as G4/G6/G8 evidence for new primitive higher-cell structure.

## GR status

No O(3), HDA, spin-2 or nonlinear GR promotion follows. Those downstream gates remain sealed.
