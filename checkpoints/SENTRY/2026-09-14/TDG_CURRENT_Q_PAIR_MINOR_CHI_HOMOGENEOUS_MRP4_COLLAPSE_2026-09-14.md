# Complete TDG / Unified Bridge Sentry — current-Q pair-minor `chi` homogeneous MRP4 collapse

**Date:** 2026-09-14 (America/Toronto)  
**Status:** **EXACT HOMOGENEOUS-PHASE ROUTE NO-GO / ANISOTROPIC CAPACITY OPEN / NO PHYSICAL MRP4 PROMOTION**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Context

Two 2026-09-14 corrections are binding before this checkpoint:

1. The old MRP4 scalar field is underdetermined if treated as a primitive positive scalar, but if Q supplies a physically distinguished positive Herm2 pair response `F_uv`, then `chi_uv=sqrt(det F_uv)` is a canonical degree-one frame-covariant response magnitude.
2. The historical MRP3/MRP4 six-label carrier is an abstract comparator and cannot be identified with current RefinedQ physical pair/subchannel labels by declaration. Current Q instead has its own retained response-Gram/Cons carrier.

The present audit therefore asks whether the **actual current-Q retained pair Gram** can supply the MRP4 positive state factor directly.

No b4/b24 target fitting, O(3), HDA, spin-2 or Einstein target is used to define the candidate.

---

## 1. Current-Q physical retained pair carrier

GR54 establishes in the tested current-Q realization:

- 101 coarse B4 parent contexts;
- 29 retained vertex labels;
- 106 retained off-diagonal B2/edge pairs;
- 135 total retained Gram pair coordinates after the full current-carrier Cons coequalizer.

Every raw B3 response Gram contributes symmetric pair values. Repeated occurrences of each diagonal or off-diagonal pair are identified by connected Cons equality graphs, and the physical retained object is the global Cons coequalizer before local restriction.

GR54 additionally derives the first retained-Gram variation from the same ordered B2 physical process as a readout-free formal unitary Taylor coefficient (`t^7`; base Gram starts at `t^4`). Thus the retained Gram is a genuine current-Q process response object, not an imported historical S6 tensor.

Q-B1186 remains binding: the Hilbert response Gram is PSD and is not by itself a Lorentzian spacetime metric.

---

## 2. Pair-minor candidate

For every physical retained pair `(u,v)` for which the current-Q Gram contains

`G_uu, G_uv, G_vv`, 

define the principal pair Gram

`G^(uv) = [[G_uu, G_uv],[G_uv, G_vv]]`.

Because the full response Gram is positive semidefinite, every such principal minor is PSD and

`det G^(uv) >= 0`.

This matches the old Q-B62/63 pair-cone fact: every coherent two-event response Gram is a positive 2x2 Hermitian object whose determinant is the natural Lorentz-cone quadratic invariant.

The scalar candidate is therefore

`chi_uv = sqrt(det G^(uv))`.

This avoids the invalid identification `chi_uv = G_uv`, because Gram off-diagonal entries need not be positive.

The candidate is a response magnitude only; it is not Born probability, factual weight or spacetime metric.

---

## 3. Exact homogeneous theorem

On an S4-central/homogeneous local retained Gram, symmetry forces

`G_uu = d` for every local label `u`,

`G_uv = c` for every distinct local pair `u!=v`.

Therefore every physical 2x2 pair minor is

`[[d,c],[c,d]]`

and

`det G^(uv) = d^2-c^2`

independent of the pair.

Hence

`chi_uv = sqrt(d^2-c^2) = chi_0`

for all local pairs.

Now insert this into the frozen MRP4 local star. Its three future-star values are equal:

`D_Fe = chi_0 I_3`.

On the sum-zero doublet,

`G_Fe = Q_F^dagger D_Fe Q_F = chi_0 I_2`.

Thus

`P_Fe = G_Fe/sqrt(det G_Fe) = I_2`.

So the MRP4 response becomes exactly

`R^chi_Fe = R^0_Fe`.

### Binding conclusion

**A current-Q pair-minor determinant magnitude built only from the exact homogeneous/S4-central retained Gram cannot supply the noncompact MRP4 factor. It collapses identically to the MRP3 uniform-response limit.**

This is an exact representation/symmetry theorem, not a numerical accident.

---

## 4. Fresh verifier

A generic positive S4-central Gram with

`d=2.3`, `c=.41`

has eigenvalues

`(1.89,1.89,1.89,3.53)`.

Fresh verifier: **6/6 PASS**.

- all pair-minor determinants equal;
- all `chi_uv` equal;
- MRP4 positive-factor identity defect `3.14e-16`;
- exact homogeneous collapse to MRP3;
- a generic anisotropic perturbation splits the pair magnitudes;
- the analytic linearized pair-magnitude formula agrees with the finite perturbation to `7.04e-11` at `eps=1e-5`.

---

## 5. Anisotropic tangent remains nontrivial

For a small symmetric perturbation around the homogeneous base,

`G_uu=d+delta d_u`,

`G_uv=c+delta c_uv`,

the first variation is

`delta chi_uv = [d(delta d_u+delta d_v)-2c delta c_uv] / [2 sqrt(d^2-c^2)]`.

Therefore anisotropic/state-conditioned retained-Gram perturbations can generate nonuniform positive pair magnitudes at first order.

This proves **capacity**, not a physical completion.

The closest existing same-process current-Q continuation is GR56. It reconstructs the actual GR54 process generator and evaluates generic anisotropic states. The raw finite anisotropic map exists, but its covariance fails at O(1):

- homogeneous all-24 raw covariance median defect `~1.173`;
- maximum `~1.431`;
- for `rho_Gibbs + eps D1`, eps `.005`: defects `1.124`, `1.284`;
- eps `.01`: defects `1.123`, `1.288`.

GR56 classifies the raw same-process generic continuation as **FINITE BUT COVARIANCE FAIL** and retains O(3)/HDA/GR closed.

This does not universally rule out every future anisotropic pair-minor response law, but it prevents promoting the obvious existing current-Q anisotropic continuation as the missing MRP4 process law.

---

## 6. Cons/nonlinearity stage-order firewall

The physical current-Q retained object is produced by the **entrywise global Cons coequalizer** on Gram coordinates.

Because determinant is nonlinear,

`det(average G_local) != average det(G_local)`

in general.

Therefore one must not average local `chi` values first and call the result the retained physical pair magnitude.

The only stage order licensed by the current response-Gram construction is:

1. perform the already-derived Cons descent/coequalizer on Gram entries;
2. construct the retained physical pair minor `G^(uv)`;
3. only then apply `sqrt(det)` if this scalar response magnitude is used.

This removes an otherwise hidden refinement ambiguity.

---

## 7. Relation to MRP4/MRP5 and G0-G10

The result sharpens the modern classification:

- old abstract S6 MRP4 pair source: **NOT PHYSICALLY TYPED IN CURRENT Q**;
- current-Q pair-minor response magnitude: **Q-NATIVE STRUCTURAL CANDIDATE**;
- homogeneous phase as noncompact MRP4 source: **EXACT FAIL / P=I**;
- anisotropic pair-minor capacity: **PASS AT LINEAR RESPONSE LEVEL**;
- current same-process generic anisotropic naturality: **FAIL in closest GR56 route**;
- static-common-cause/process-identifiability null: **not defeated by this checkpoint**.

G0 remains open. No new microscopic law is admitted.

---

## 8. Exact next legal gate

### CURRENT-Q ANISOTROPIC PAIR-MINOR RESPONSE NATURALITY / PROCESS-NORMALITY GATE

A legal continuation must stay on the actual current-Q carrier and test, before any GR-facing target:

1. derive a state/process-conditioned anisotropic retained Gram directly from the intervention-complete current-Q process;
2. preserve the full global Cons coequalizer and carrier automorphisms;
3. construct `chi_uv=sqrt(det G^(uv))` only after Cons descent;
4. verify the induced pair-magnitude law transforms naturally under current-Q automorphisms rather than through a hand Reynolds repair;
5. test whether the resulting nonuniform `chi` has positive margin against a frozen static-common-cause model carrying the same pair magnitudes;
6. test spectators, process breaks and refinement/dilation controls;
7. only if these pass, build the local projective response factor on the actual current carrier and freeze it before any P10/ELGC/O(3) test.

If the natural anisotropic response cannot be derived, the MRP mechanism remains capacity evidence rather than physical Root2 content.

---

## 9. Petit verdict — avance vers GR ?

🟢 **Yes, in localization and type cleanup.** A genuinely current-Q pair-response scalar candidate now exists, and its homogeneous behavior is solved exactly.

🟡 **The only potentially useful sector is state-conditioned anisotropy.** Its first-order scalar capacity is explicit, but the closest current-Q generic continuation fails covariance strongly.

🔴 **No physical MRP promotion and no GR closure.** Homogeneous pair-minor MRP4 collapses exactly to MRP3; G0/G2/G4/G6 remain open; O(3), HDA, spin-2 and nonlinear GR remain not established.
