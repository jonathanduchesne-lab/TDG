# Complete TDG / Unified Bridge Sentry — current-Q pair-minor `chi` static-common-cause identifiability no-go

**Date:** 2026-09-14 (America/Toronto)  
**Status:** **EXACT DESCRIPTOR-LEVEL IDENTIFIABILITY NO-GO / NATURALITY RETAINED / ORDER-SENSITIVE PROCESS DATA REQUIRED**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Parent result

The preceding current-Q pair-minor checkpoint established the candidate

`chi_uv = sqrt(det G^(uv))`

from the actual retained response-Gram/Cons carrier, with Cons descent applied before the nonlinear determinant.

At the exact homogeneous S4-central point all `chi_uv` are equal and the MRP4 positive factor collapses to identity. Anisotropic perturbations generate nonuniform `chi_uv` at first order.

The present gate asks whether that anisotropic pair-minor descriptor can pass the required matched static-common-cause / process-identifiability control.

No O(3), HDA, spin-2 or Einstein target is used.

---

## 1. Exact theorem: every PSD Gram has a static feature realization

Let `G` be any positive-semidefinite retained response Gram on a finite set of physical labels.

By Gram/Cholesky factorization there exist static feature vectors `{x_u}` in some Hilbert space such that

`G_uv = <x_u,x_v>`.

For example, if `G=L L^dagger`, take the static feature matrix `X=L^dagger`, so `X^dagger X=G`.

This model contains no ordered transition law. It is a static common-cause/feature realization of exactly the same Gram data.

For every physical pair `(u,v)`, the principal 2x2 minor is therefore identical:

`G_static^(uv) = G_process^(uv)`.

Hence

`chi_uv^static = sqrt(det G_static^(uv)) = sqrt(det G_process^(uv)) = chi_uv^process`.

Any downstream construction that depends only on the labelled `chi` field and fixed incidence/combinatorial data is therefore exactly identical in the process and static realizations.

This includes any MRP4-style positive polar factor built only from three or more selected `chi` values.

### Binding conclusion

**Pair-minor `chi` cannot identify ordered process structure.**

Anisotropy does not change this theorem: a generic anisotropic PSD Gram still admits the same static realization.

---

## 2. Naturality is not the problem

For a carrier relabeling/permutation `p`, the Gram transforms by congruence

`G -> P G P^T` (or `P G P^dagger` in the Hermitian case).

The principal pair minor on the relabeled pair is exactly the old principal pair minor with relabeled indices. Therefore

`chi_{ij}(P G P^T) = chi_{p(i)p(j)}(G)`.

So pair-minor `chi` is automatically relabel-natural whenever the retained Gram itself is natural.

This requires no Reynolds averaging and is categorically upstream of the GR56 `common3 -> Sym4` solder covariance failure.

GR54 already supplies exact automorphisms of the complete current carrier and exact Cons pair coequalization; Q-B1183/Q-B1184 provide the local/full-future Gram process-sheaf covariance structure. The descriptor-level relabel theorem is therefore compatible with the retained current-Q architecture.

---

## 3. Dilation and spectator behavior

The no-go is strengthened, not weakened, by standard harmless transformations.

### Isometric dilation

If static/process feature vectors are embedded isometrically by `W^dagger W=I`, then

`G -> X^dagger W^dagger W X = G`.

All pair-minor `chi` values are unchanged exactly.

### Independent spectator

For an orthogonal direct-sum spectator,

`G_total = G_active direct_sum G_spectator`,

every old active pair minor is unchanged. Thus old pair-minor `chi` values are spectator-blind exactly.

These are good consistency properties but provide **zero identifying margin** between a dynamical process realization and a static feature realization.

---

## 4. Fresh numerical verifier

### Real anisotropic SPD audit — 5/5 PASS

A generic 7x7 anisotropic SPD Gram was generated before evaluating the null.

- minimum eigenvalue: `0.7000625173`;
- pair-`chi` spread: `13.44279404`;
- Cholesky static Gram relative defect: `2.42e-17`;
- static-vs-process pair-`chi` maximum defect: `5.33e-15`;
- relabel naturality maximum defect: `8.88e-16`;
- representative downstream 2x2 positive polar factor defect: `0.0`.

### Independent complex-Hermitian audit — 5/5 PASS

A generic 6x6 complex Hermitian SPD Gram gives:

- minimum eigenvalue: `0.7819903017`;
- pair-`chi` spread: `8.439714497`;
- static factorization relative defect: `1.27e-16`;
- static-vs-process `chi` defect: `1.24e-14`;
- isometric-dilation `chi` defect: `5.33e-15`;
- independent-spectator old-pair `chi` defect: `0.0`.

Thus the result is not tied to a real-symmetric special case.

---

## 5. G0-G10 consequence

For the current-Q `Gram -> pair-minor -> chi` route as a proposed new microscopic/process enrichment:

- G0 necessity: unchanged OPEN;
- G1 physical typing: **STRUCTURAL PASS** as a current-Q response descriptor after Cons;
- G2 nonredundancy/process identification: **EXACT FAIL** — static common cause reproduces every `chi` exactly;
- G3 composition-normality: descriptor may be mathematically composable, but it carries no intrinsically ordered process information and cannot by itself certify a process law;
- G4 dilation-normality: **PASS as invariance**, but also demonstrates descriptor nonidentifiability;
- G5 no fit: PASS;
- G6 matched-null margin: **EXACT ZERO**;
- G7 relabel/Cons naturality: structural PASS conditional on the retained Gram naturality already present upstream;
- G8 refinement survival: may be tested as a descriptor but cannot repair G2/G6;
- G9 conservative Q: PASS as read-only descriptor;
- G10 pretarget freeze: PASS.

Therefore this route is **NOT ADMISSIBLE NEW TDG MICROPHYSICS** even in the anisotropic sector.

---

## 6. What kind of object can still beat the null?

Any candidate constructed solely from a symmetric positive Gram is vulnerable to the same factorization theorem.

A surviving process-identifying candidate must contain information not reconstructible from one static Gram. Natural possibilities already present in the Q lineage include:

1. **ordered two-step / multitime response data**;
2. **antisymmetric/Lie/commutator content** of the full-Herm2 ordered process-comb;
3. history-conditioned response that passes the pair-vs-memory/dilation firewalls;
4. a higher cell or boundary/inflow law whose composition order is physical and not reducible to a static feature Gram.

Q-B1859 LIVE plus BF/BH already identify the richer same-Q ordered full-Herm2 Lie/Jordan process-comb as the correct upstream object relative to normalized scalar/Gram reductions. This is therefore the next legal source family to audit.

---

## 7. Exact next gate

### ORDER-SENSITIVE CURRENT-Q PROCESS RESPONSE / STATIC-GRAM-SEPARATION GATE

Before any P10/ELGC/O(3)/GR-facing comparison:

1. recover the already-derived ordered full-Herm2 two-step/process-comb object (BF/BH / Q-B1859 lineage);
2. separate symmetric Jordan/Gram-like content from antisymmetric Lie/commutator content;
3. freeze a target-blind process descriptor before downstream use;
4. require a positive matched margin against every static single-Gram realization with the same one-step Gram/pair-minor data;
5. require relabel/Cons covariance, independent spectators, process-order reversal, and dilation controls;
6. require the descriptor to vanish or transform predictably under a frozen process-break/null model;
7. only if such an order-sensitive descriptor survives may it be considered as input to a noncompact/projective response law under G0-G10.

No choice of O(3), GR residual, tetrad or geometric target may select the descriptor.

---

## 8. Petit verdict — avance vers GR ?

🟢 **Strong diagnostic progress.** The anisotropic pair-minor route is now fully classified: natural and physically readable, but not process-identifying.

🟡 **The frontier moves upstream to ordered/multitime Q data**, especially the already-derived full-Herm2 Lie/Jordan process-comb.

🔴 **No MRP promotion and no GR closure.** A symmetric Gram, however anisotropic, cannot by itself distinguish a dynamical process from a static common cause; G2/G6 fail exactly.
