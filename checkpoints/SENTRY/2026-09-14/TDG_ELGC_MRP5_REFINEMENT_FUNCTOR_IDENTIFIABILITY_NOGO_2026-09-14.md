# Complete TDG / Unified Bridge Sentry — ELGC + modern MRP5 refinement-functor identifiability

**Date:** 2026-09-14 (America/Toronto)  
**Status:** **CLOSED STAGE-I IDENTIFIABILITY NEGATIVE / MRP4 REFINEMENT LAW UNDERDETERMINED / ELGC RETAINED AS AUTHOR POSTULATE ONLY**  
**Historical FINAL-CERTIFIED authority:** `Q-B1858L`, unchanged.

## 0. Purpose and latest-wins scope

This checkpoint resumes the historical MRP4 -> MRP5 branch under the stricter 2026-09-10 G0-G10 new-microphysics gate and the later author-approved ELGC sandbox.

Binding corrections before this audit:

- P10I/P10J corrected the old GR189 `rcoord_series` bug and restored the D4 affine pencil to rank 4. The old rank-3/fourth-null interpretation is revoked.
- P10M->P10S remains a strong microscopic cofinality adversary, but the strict-G0 2026-09-11 reclassification states that finite b4/b24 noncofinality is **not by itself** a new-physics witness.
- Q-B1857 supersedes the older Q-B1856 ordinary-coordinate shared-B3 rank-2/M3 obstruction: that pair tensor is generated by the Cons quotient normalization `m=S/N` and is not an independent missing constitutive law.
- ELGC is an explicit Jonathan-approved working postulate, not a theorem of the old Root and not automatic G0-G10 admission for a concrete Q modification.

No b4/b24, O(3), HDA, spin-2, Einstein, preferred-z or GR residual is used in the present MRP5 audit.

---

## 1. Frozen historical MRP4 object

MRP4 freezes a strictly positive labelled pair-joint selected-future response ray

`chi_uv > 0`

on the 15 unordered pairs of six carrier labels. For every incidence `e subset F`, with distinguished `u=F\e` and future alternatives `v in F^c`, it defines

`D_Fe(chi)=diag(chi_uv)`,

`G_Fe(chi)=Q_F^dagger D_Fe(chi) Q_F`,

`P_Fe(chi)=G_Fe(chi)/sqrt(det G_Fe(chi))`,

`R^chi_Fe=P_Fe(chi) R^0_Fe`,

`T^chi_Fe=[R^chi_Fe] in PGL(2,C)`.

Historical MRP4 retained:

- exact positive determinant-one local factor;
- common-scalar blindness;
- exact S6 covariance;
- two-dimensional noncompact local tangent;
- generic formal response-pencil span > 1;
- preservation and activation of the five MRP3 kernel-origin modes;
- rejection of pure endpoint-gauge explanation;
- Hermitian microscopic block-process compatibility.

But MRP4 did **not** derive refinement functoriality/cofinality and was never adopted as final physical Q.

A later matched control additionally proved that any frozen unordered `chi` plus deterministic MRP4 response geometry can be reproduced by a static common-cause/direct-product base. Thus MRP4 alone has zero ordered-process identifying margin.

---

## 2. Exact modern MRP5 question

Can the already-frozen MRP4 typing of `chi` uniquely determine a positive, compositionally functorial coarse/fine law before any downstream target is consulted?

Required structural controls are exactly the spirit of historical MRP5:

1. positivity;
2. positive homogeneity/common-scalar covariance;
3. nested refinement-diamond functoriality;
4. relabel covariance;
5. independent-spectator projective blindness;
6. no deletion of the five MRP3 kernel-origin modes;
7. no pseudoinverse or singular-boundary repair;
8. no b4/b24/O3/GR fitting.

The audit tests whether these conditions identify one physical refinement functor.

---

## 3. Exact counterfamily

Let a fine positive pair-response field be `chi_ab>0` and let a coarse label `A` denote a fibre of fine labels. For every real `p>0`, define

`chi^(p)_{AB} = ( sum_{a in A, b in B} chi_ab^p )^(1/p)`.

This is the ordinary `l_p` pushforward of the positive pair-response field.

### 3.1 Positivity

For positive fine `chi`, every coarse `chi^(p)` is positive.

### 3.2 Positive homogeneity

For any common `lambda>0`,

`PF_p(lambda chi) = lambda PF_p(chi)`.

Since MRP4 is projectively blind to common local scale, this passes the frozen scalar-response-factor control.

### 3.3 Exact refinement diamonds

For nested partitions fine -> middle -> coarse,

`PF_p^(middle->coarse) o PF_p^(fine->middle) = PF_p^(fine->coarse)`

exactly, because raising the middle result to the p-th power reconstructs the sum of the fine p-th powers.

Thus every `p>0` is exactly compositionally associative/functorial on refinement diamonds.

### 3.4 Relabel covariance

The definition depends only on the fibres and the positive pair data; it commutes exactly with relabeling.

### 3.5 Independent spectators

If an independent spectator multiplies fine responses by a factorized positive spectator weight, the `l_p` pushforward contributes one common positive factor to all relevant coarse pair responses. MRP4 removes that factor projectively. Thus independent spectators do not select p.

---

## 4. Fresh numerical audit

A 24-label nested refinement was built as

`6 coarse x 2 middle x 2 fine`.

A generic positive fine pair field was frozen before evaluating the coarse MRP4 responses.

Tested `p = 1,2,3`.

### 4.1 Exact/numerical structural passes

For all three p values:

- nested-diamond relative defect <= `2.42e-16`;
- relabel covariance defect = `0.0` in the tested nontrivial permutation;
- common-scalar covariance defect <= `1.97e-16`;
- independent-spectator projective residual <= `1.89e-16`;
- coarse MRP4 coupling rank = `30`;
- image rank of the frozen five-dimensional MRP3 kernel-origin subspace = `5`;
- smallest singular value of that five-mode image remains nonzero (`0.0777`, `0.0809`, `0.0839` for p=1,2,3 respectively).

Thus all three laws satisfy the structural MRP5 controls used here and preserve the five historical modes.

### 4.2 The laws are physically inequivalent inside MRP4

They are not related by one harmless common scalar.

Pairwise coarse `chi` projective residuals:

- p1 vs p2: `0.00976027`;
- p1 vs p3: `0.01911297`;
- p2 vs p3: `0.00940066`.

Corresponding common-scalar residuals of the full MRP4 coupling `C(chi)`:

- p1 vs p2: `0.00597279`;
- p1 vs p3: `0.01183113`;
- p2 vs p3: `0.00588927`.

Maximum local projective `R^chi_Fe` differences:

- p1 vs p2: `0.01002221`;
- p1 vs p3: `0.02062704`;
- p2 vs p3: `0.01061417`.

So the ambiguity changes the actual projective response transport; it is not a descriptive global normalization.

---

## 5. Independent verifier

A completely separate refinement-only implementation used

`6 coarse x 3 middle x 2 fine = 36 fine labels`

with a fresh random positive field and tested

`p = 1, 1.5, 2.5, 4`.

Result: **10/10 PASS**.

Nested-diamond relative defects were all <= `2.15e-16` while the resulting coarse response rays remained distinct; the p1-vs-p4 projective separation was `0.0521053`.

This independently confirms that the nonuniqueness is not tied to one state, one partition multiplicity or the p={1,2,3} sample.

---

## 6. Why ordinary CP direct-sum additivity does not silently rescue uniqueness

If `chi` is **additionally** identified as an additive positive CP effect/subchannel weight, then ordinary direct-sum coarse-graining requires linear addition:

`chi_{AB} = sum_{a in A,b in B} chi_ab`,

i.e. p=1 in the counterfamily.

The fresh audit confirms this sharply. For a representative coarse pair, relative error from ordinary linear additivity is

- p=1: `2.15e-16`;
- p=2: `0.7461`;
- p=3: `0.8376`.

The independent verifier gives the same classification for p=1 vs p=1.5,2.5,4.

However MRP4 did **not** derive this physical identification. Its certified statement was only:

- any positive `chi` ray can be normalized and represented compatibly by labelled positive/Kraus response amplitudes;
- no statement identifies those coefficients with factual probabilities;
- final physical Q adoption remains NO.

Compatibility with an additive CP representation is weaker than a theorem that `chi` **is** the additive CP effect/weight whose direct sums must add linearly.

Promoting p=1 for that reason would therefore add a new physical typing axiom for `chi`.

---

## 7. Static-common-cause firewall remains binding

Even if one explicitly chose p=1, a deterministic refinement rule for a frozen unordered `chi` does not by itself create an ordered microscopic process normal.

The earlier MRP4 matched control still applies: a static latent common cause carrying the complete `chi` field can reproduce the deterministic MRP4 link matrix, formal odd powers and holonomy descendants exactly over a commuting direct-product base.

Therefore MRP5 refinement functoriality alone cannot supply the missing AF/direct-product-normal margin or a history-to-Q promotion law.

The naive `M18 -> chi` graft remains forbidden by the separate no-backflow / MRP4+M18 no-go.

---

## 8. Relation to ELGC and strict G0

ELGC remains useful as an author-approved **final-law admissibility principle**:

- if Q supplies a regular phase with little group `G_Q` and no physical breaking order parameter, a final universal constitutive law must be `G_Q`-covariant;
- a candidate may alternatively derive physical transforming order data and covariantize through it.

But ELGC does not prove that the old Root must be microscopically enriched rather than completed by a derived effective quotient/transport, and it does not choose one `chi` ontology.

Therefore the precise modern classification is:

- `ELGC final-law discriminator` = **RETAINED AUTHOR POSTULATE**;
- `ELGC => unique MRP4 chi refinement law` = **FAIL**;
- `MRP4 typing alone => G1 physical refinement typing complete` = **FAIL / UNDERDETERMINED**;
- `MRP5 composition/naturality` = **NONUNIQUE FAMILY / NOT CLOSED**;
- `strict G0 new-microphysics necessity from old Root` = **NOT DERIVED**;
- `P10S` remains a held-out cofinality adversary, not the witness used to choose the law.

---

## 9. G0-G10 scorecard for historical MRP4 under the modern gate

This is a reclassification, not an adoption.

- G0 pretarget new-microphysics necessity: **OPEN / NOT ROOT-DERIVED**; ELGC is an explicit extra author premise, not a theorem.
- G1 physical typing: **PARTIAL**; positive projective pair-response ray is typed, but additive-effect vs norm/amplitude/refinement ontology is not fixed.
- G2 nonredundancy/dilation normality: **OPEN/FAIL TO IDENTIFY**; static-common-cause/direct-product matched control remains exact for the frozen observable package.
- G3 composition-normality: **UNDERDETERMINED**; continuum `p>0` refinement family passes structural diamonds.
- G4 dilation-normality: **NOT PASSED**.
- G5 parameter selection independent of GR: **p is not selected at all**; no target fit performed.
- G6 matched-null identifying margin: **ZERO for current frozen MRP4 package**.
- G7 Cons/composition naturality: **PARTIAL structural pass, physical functor not unique**.
- G8 refinement survival: **many candidate functors survive; uniqueness FAIL**.
- G9 old-Q consistency/conservative limit: **historical comparator passes Hermitian/no-spurion controls, but final physical-Q adoption remains NO**.
- G10 Stage-I freeze: **PASS for this audit**; no downstream target was opened.

Hence MRP4 is **NOT ADMISSIBLE NEW TDG MICROPHYSICS** under the modern G0-G10 gate.

---

## 10. Exact next legal question

The next question is now narrower than historical MRP5:

> **Can Q independently define what `chi` physically is — specifically an additive CP effect/subchannel weight, a response norm, an amplitude-derived quantity, or another intervention-complete object — in a way that fixes its refinement pushforward before any geometry/cofinality target?**

If Q derives additive CP-effect typing, p=1 becomes a legal consequence and MRP5 can be resumed with that frozen law.

If Q does not fix this ontology, choosing p=1 is a new Root2 physical postulate and must be labelled/admitted as such.

Only after that typing is fixed may the resulting MRP4/MRP5 candidate face P10M/P10O/P10P as held-out cofinality adversaries and, much later, the ELGC/O(3) downstream gate.

---

## 11. Petit verdict — avance vers GR ?

🟢 **Oui, en localisation scientifique.** We found an exact reason historical MRP5 could not close: the response-state refinement law is physically under-typed, not merely computationally unfinished.

🟡 **No direct GR closure.** ELGC still gives a clean criterion for a final regular-phase law, but it does not select the missing microscopic/refinement ontology.

🔴 **MRP4 is not promoted.** No b4/b24 or O(3) target was consulted, the static-common-cause null remains live, and HDA/spin-2/nonlinear GR remain not established.
